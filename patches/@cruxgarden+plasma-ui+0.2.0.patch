diff --git a/node_modules/@cruxgarden/plasma-ui/dist/index.js b/node_modules/@cruxgarden/plasma-ui/dist/index.js
index d4aa606..d1f0e04 100644
--- a/node_modules/@cruxgarden/plasma-ui/dist/index.js
+++ b/node_modules/@cruxgarden/plasma-ui/dist/index.js
@@ -480,7 +480,7 @@ var PlasmaRenderer = class _PlasmaRenderer {
       const list = [];
       this.recs.forEach((r) => {
         if (!r.el.isConnected) return;
-        if (!s.reducedMotion) {
+        if (!s.reducedMotion && s.animateSurfaces !== false) {
           r.formV += (170 * (1 - r.form) - 12 * r.formV) * dt;
           r.form += r.formV * dt;
         } else r.form = 1;
@@ -522,7 +522,7 @@ var PlasmaRenderer = class _PlasmaRenderer {
           r.onSides?.(sides);
         }
         let tx = 0, ty = 0;
-        if (r.lean > 0 && !joined && !r.dragging && !s.reducedMotion) {
+        if (r.lean > 0 && !joined && !r.dragging && !s.reducedMotion && s.animateSurfaces !== false) {
           const dx = m.tx - (a.l + a.w / 2), dy = m.ty - (a.t + a.h / 2);
           const pull = Math.exp(-(dx * dx + dy * dy) / 12e4) * r.lean * m.amt;
           const len = Math.hypot(dx, dy) || 1;
@@ -534,7 +534,7 @@ var PlasmaRenderer = class _PlasmaRenderer {
         r.ly += (ty - r.ly) * le;
         r.el.style.translate = `${r.lx.toFixed(2)}px ${r.ly.toFixed(2)}px`;
         const u = (this.time - r.pulseAt) / 0.36;
-        if (r.pulseAt >= 0 && u >= 0 && u <= 1 && !s.reducedMotion) r.el.style.scale = String(1 + 0.04 * r.pulseS * Math.sin(Math.PI * u));
+        if (r.pulseAt >= 0 && u >= 0 && u <= 1 && !s.reducedMotion && s.animateSurfaces !== false) r.el.style.scale = String(1 + 0.04 * r.pulseS * Math.sin(Math.PI * u));
         else if (r.el.style.scale) r.el.style.scale = "";
       });
       const v = Math.min(Math.max(s.viscosity, 0), 1);
@@ -549,7 +549,7 @@ var PlasmaRenderer = class _PlasmaRenderer {
         const rr = r.el.getBoundingClientRect();
         const tgt = [rr.left + sx, rr.top + sy, rr.right + sx, rr.bottom + sy];
         const sp = r.sp;
-        if (!sp.live || st < 0.01 || s.reducedMotion) {
+        if (!sp.live || st < 0.01 || s.reducedMotion || s.animateSurfaces === false) {
           sp.e = tgt.slice();
           sp.v = [0, 0, 0, 0];
           sp.live = true;
@@ -705,7 +705,7 @@ var PlasmaRenderer = class _PlasmaRenderer {
       id,
       el,
       ...o,
-      form: this.settings.reducedMotion ? 1 : 0,
+      form: this.settings.reducedMotion || this.settings.animateSurfaces === false ? 1 : 0,
       formV: 0,
       removing: false,
       lx: 0,
@@ -879,6 +879,20 @@ var PlasmaRenderer = class _PlasmaRenderer {
     gl.useProgram(bl.pr);
     gl.uniform1i(bl.u.uTex, 0);
     gl.activeTexture(gl.TEXTURE0);
+    // Tigrana: blur only the background texture, before drawing panel masks,
+    // lighting and refraction. Reuse scratch targets; Sharp adds no passes.
+    const backgroundBlur = Math.min(40, Math.max(0, s.backgroundBlur || 0));
+    if (backgroundBlur > 0) {
+      const step = backgroundBlur * this.dpr * MASK_SCALE / 4;
+      pass(this.rtBg, this.rtBgM, 0, 0);
+      for (let n = 0; n < 3; n++) {
+        pass(this.rtBgM, this.rtB, step, 0);
+        pass(this.rtB, this.rtBgM, 0, step);
+      }
+      gl.viewport(0, 0, this.rtBg.w, this.rtBg.h);
+      pass(this.rtBgM, this.rtBg, 0, 0);
+      gl.viewport(0, 0, this.rtA.w, this.rtA.h);
+    }
     if (list.some((_, i) => this.FR[i] > 2e-3)) {
       pass(this.rtBg, this.rtBgM, 1.5 * k, 0);
       pass(this.rtBgM, this.rtB, 0, 2.5 * k);
diff --git a/node_modules/@cruxgarden/plasma-ui/dist/renderer.d.ts b/node_modules/@cruxgarden/plasma-ui/dist/renderer.d.ts
index 1634c04..b9d472a 100644
--- a/node_modules/@cruxgarden/plasma-ui/dist/renderer.d.ts
+++ b/node_modules/@cruxgarden/plasma-ui/dist/renderer.d.ts
@@ -1,5 +1,9 @@
 import { Box } from "./snap";
 export interface RendererSettings {
+    /** Tigrana: false keeps panel geometry fixed to DOM bounds, without slowing the background. */
+    animateSurfaces?: boolean;
+    /** Tigrana: background-only blur radius in CSS pixels, 0–40. */
+    backgroundBlur?: number;
     colors: [string, string, string];
     blend: number;
     refraction: number;
