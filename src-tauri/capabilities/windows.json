{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "windows-chrome",
  "description": "Windows custom title bar controls.",
  "platforms": ["windows"],
  "windows": ["main", "tigrana-*"],
  "permissions": ["core:window:allow-minimize", "core:window:allow-close"]
}
